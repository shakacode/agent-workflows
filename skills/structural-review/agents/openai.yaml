# Codex UI metadata for skill picker display text and default prompt.
interface:
  display_name: "Structural Review"
  short_description: "Review a diff for structural drift"
  default_prompt: "Use $structural-review to check whether a change makes the codebase structurally worse."
