# Codex UI metadata for skill picker display text and default prompt.
interface:
  display_name: "PR Lane"
  short_description: "Run one coordinated PR lane"
  default_prompt: "Use $pr-lane to run one direct-prompt issue, pull request, or ad-hoc task as a coordinated PR lane."
