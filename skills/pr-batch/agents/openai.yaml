# Codex UI metadata for skill picker display text and default prompt.
interface:
  display_name: "PR Batch"
  short_description: "Run one or more coordinated PR lanes"
  default_prompt: "Use $pr-batch to run one or more approved issue, PR, or ad-hoc task lanes with coordinated subagents."
