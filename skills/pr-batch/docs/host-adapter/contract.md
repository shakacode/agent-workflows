# Host Adapter Contract

Prompt compatibility protocol: 1

This skill-local copy exists so isolated `skills/pr-batch` installs can verify
prompt compatibility without a repo-root docs tree. When the full pack is
available, the canonical contract remains `docs/host-adapter/contract.md`.
